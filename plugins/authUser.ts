export default defineNuxtPlugin(async (nuxtApp) => {
    
//    useApiFetch("/user").catch(err => {
//     throw createError({
//       statusCode: err?.value?.statusCode,
//       statusMessage:  err?.value?.statusMessage,
//     });
//    })
    
});